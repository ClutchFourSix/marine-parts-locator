export async function runCheerioSource(source) {
  console.log(`Cheerio crawler scaffold for source: ${source.name}`);
  console.log('Next step: implement Crawlee CheerioCrawler with extraction logic.');
}
